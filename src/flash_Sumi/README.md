# TO-DO
To run the test run:

//This makes a symlink for both folders before testing
cd /mnt/c/Users/mukte/Documents/CSE127/NoCNoC
ln -s "mesh_psi_aan" src/mesh
ln -s "dft - Ethan" src/dft
ln -s "dft_ethan" src/dft
ls src/mesh/
ls src/dft/

make sim-flash 